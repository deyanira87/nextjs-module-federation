{
  "name": "nextjs-ssr",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm --parallel --filter './microfrontends/*' dev",
    "build": "pnpm --parallel --filter './microfrontends/*' build",
    "start": "pnpm --parallel --filter './microfrontends/*' start"
  },
  "devDependencies": {
    "@module-federation/typescript": "^3.1.3",
    "@types/webpack": "^5.28.5",
    "concurrently": "^8.2.2",
    "wait-on": "^7.2.0",
    "webpack": "^5.97.1"
  }
}
